# OFD Viewer 架构设计

## 1. 架构概述

OFD Viewer 采用分层架构设计，将 OFD 文档处理功能划分为多个相互协作的模块，每个模块负责特定的功能领域。这种设计使得系统具有良好的可扩展性、可维护性和可测试性。

## 2. 整体架构设计

### 2.1 分层结构

OFD Viewer 采用经典的三层架构设计：

1. **表示层**：提供对外的 API 接口，方便用户使用
2. **业务逻辑层**：处理核心业务逻辑，包括 OFD 文档的读取、解析和写入
3. **数据访问层**：负责 OFD 文档的底层存储和访问

### 2.2 核心组件关系

```
┌─────────────────────────────────────────────────────────────────┐
│                        表示层                                    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                     OFDDocument                        │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        业务逻辑层                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────┐  │
│  │  OFDReader  │  │  OFDWriter  │  │ PageDocument│  │SignDocument│ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据访问层                                │
│  ┌─────────────┐  ┌───────────────────────────────────┐         │
│  │ OFDArchive  │  │             Models                │         │
│  └─────────────┘  │ ┌─────┐ ┌─────┐ ┌─────┐ ┌───────┐ │         │
│                   │ │Font │ │Graph│ │Image│ │Signature│ │         │
│                   │ └─────┘ └─────┘ └─────┘ └───────┘ │         │
│                   └───────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        工具层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────┐  │
│  │ XmlHelper   │  │EnumHelper   │  │SignedValueHandler│  │XmlValidator│ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 3. 核心组件详解

### 3.1 OFDDocument

**职责**：OFD 文档的核心类，提供文档的整体管理，是对外的主要 API 入口。

**主要功能**：
- 文档的打开和创建
- 页面管理
- 资源管理
- 文档的保存

**设计特点**：
- 采用工厂模式提供静态方法 `Open` 和 `Create` 来创建文档实例
- 实现 `IDisposable` 接口，确保资源正确释放
- 提供简洁易用的 API 接口

### 3.2 OFDArchive

**职责**：负责 OFD 文档的归档处理，包括 ZIP 压缩、解压、文件管理等。

**主要功能**：
- OFD 文档的打开和创建（从文件或流）
- 内部文件的读取和写入
- XML 文档的缓存管理
- 临时文件的处理

**设计特点**：
- 支持两种模式：文件模式和流模式
- 采用缓存机制提高 XML 文档的访问效率
- 实现 `IDisposable` 接口，确保资源正确释放

### 3.3 OFDReader

**职责**：负责 OFD 文档的读取和解析，将 OFD 文档的 XML 结构转换为内存中的对象模型。

**主要功能**：
- 解析 OFD 文档的基本结构
- 解析页面内容
- 解析字体信息
- 解析图形元素
- 解析注释信息
- 解析附件信息

**设计特点**：
- 采用模块化设计，便于扩展支持新的 OFD 元素
- 支持按需解析，提高性能
- 提供详细的错误处理机制

### 3.4 OFDWriter

**职责**：负责 OFD 文档的写入和生成，将内存中的对象模型转换为 OFD 文档的 XML 结构。

**主要功能**：
- 生成 OFD 文档的基本结构
- 生成页面内容
- 生成字体信息
- 生成图形元素
- 生成注释信息
- 生成附件信息

**设计特点**：
- 采用模块化设计，便于扩展支持新的 OFD 元素
- 支持增量写入，提高性能
- 提供详细的错误处理机制

### 3.5 PageDocument

**职责**：负责 OFD 页面文档的处理。

**主要功能**：
- 页面内容的读取和解析
- 页面内容的写入和生成
- 页面资源的管理

### 3.6 SignDocument

**职责**：负责 OFD 数字签名文档的处理。

**主要功能**：
- 签名信息的读取和解析
- 签名信息的写入和生成
- 签名验证

### 3.7 Models

**职责**：包含 OFD 文档的所有数据模型，按照 OFD 标准的结构组织。

**主要分类**：
- **BaseType**：基础数据类型，如字符串、数字、日期等
- **BaseStructure**：基础结构类型，如坐标、矩形、颜色等
- **Font**：字体相关模型
- **Graph**：图形相关模型
- **Image**：图片相关模型
- **Composite**：复合元素模型
- **Annotation**：注释相关模型
- **Attachment**：附件相关模型
- **Signature**：数字签名相关模型
- **Action**：动作相关模型
- **CustomTag**：自定义标签相关模型
- **Extension**：扩展相关模型
- **Version**：版本相关模型

### 3.8 Utils

**职责**：提供各种工具类，支持核心功能的实现。

**主要工具**：
- **XmlHelper**：XML 文档的辅助处理
- **EnumHelper**：枚举类型的辅助处理
- **SignedValueHandler**：签名值的处理
- **XmlRequiredValidator**：XML 必需属性的验证

## 4. 核心流程

### 4.1 OFD 文档读取流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        读取 OFD 文档                            │
├─────────────────────────────────────────────────────────────────┤
│  1. 调用 OFDDocument.Open() 方法                               │
│  2. 创建 OFDArchive 实例，打开 OFD 文档                        │
│  3. 解析文档根目录下的 OFD.xml 文件                           │
│  4. 解析文档的基本信息和结构                                   │
│  5. 解析每个页面的内容                                         │
│  6. 将解析结果转换为对象模型                                   │
│  7. 返回 OFDDocument 实例给调用者                              │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 OFD 文档写入流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        写入 OFD 文档                            │
├─────────────────────────────────────────────────────────────────┤
│  1. 调用 OFDDocument.Create() 方法                             │
│  2. 创建 OFDArchive 实例，创建新的 OFD 文档                    │
│  3. 生成文档根目录下的 OFD.xml 文件                           │
│  4. 生成文档的基本信息和结构                                   │
│  5. 生成每个页面的内容                                         │
│  6. 将对象模型转换为 XML 结构                                 │
│  7. 压缩生成 OFD 文档                                          │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 页面解析流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        页面解析流程                            │
├─────────────────────────────────────────────────────────────────┤
│  1. 读取页面的 XML 文件                                        │
│  2. 解析页面的基本属性（尺寸、方向等）                         │
│  3. 解析页面的图层结构                                         │
│  4. 解析每个图层的内容：                                       │
│     ├── 文本内容                                               │
│     ├── 图形元素                                               │
│     ├── 图片元素                                               │
│     ├── 注释元素                                               │
│     └── 其他元素                                               │
│  5. 解析页面的资源引用                                         │
│  6. 返回页面对象模型                                           │
└─────────────────────────────────────────────────────────────────┘
```

## 5. 数据模型设计

### 5.1 模型设计原则

- **符合 OFD 标准**：模型设计严格遵循 OFD 标准（GB/T 33190-2016）
- **分层设计**：基础类型、基础结构、复杂类型分层设计
- **可扩展性**：支持扩展新的 OFD 元素类型
- **易用性**：提供简洁易用的 API 接口

### 5.2 模型关系

```
┌─────────────────────────────────────────────────────────────────┐
│                      核心模型关系                               │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐     │
│  │ BaseType│────▶│BaseStructure│───▶│  Font   │     │  Image  │   │
│  └─────────┘     └─────────┘     └─────────┘     └─────────┘     │
│        │             │                  │             │         │
│        ▼             ▼                  ▼             ▼         │
│  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐     │
│  │  Graph  │     │Composite│◀────│ PageDesc│     │Signature│     │
│  └─────────┘     └─────────┘     └─────────┘     └─────────┘     │
│        │             │                  │             │         │
│        ▼             ▼                  ▼             ▼         │
│  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐     │
│  │Annotation│     │Attachment│     │  Action │     │CustomTag│    │
│  └─────────┘     └─────────┘     └─────────┘     └─────────┘     │
└─────────────────────────────────────────────────────────────────┘
```

## 6. 扩展性设计

### 6.1 扩展支持的 OFD 元素

OFD Viewer 采用模块化设计，便于扩展支持新的 OFD 元素类型：

1. **添加模型类**：在 Models 目录下添加新的模型类
2. **扩展解析器**：在 OFDReader 中添加对应的解析逻辑
3. **扩展写入器**：在 OFDWriter 中添加对应的写入逻辑

### 6.2 扩展支持的 OFD 版本

OFD Viewer 设计时考虑了版本兼容性，便于支持不同版本的 OFD 标准：

1. **版本检测机制**：自动检测 OFD 文档的版本
2. **版本适配层**：提供不同版本的适配逻辑
3. **向后兼容**：支持旧版本的 OFD 文档

## 7. 性能考虑

### 7.1 缓存机制

- **XML 缓存**：OFDArchive 类中使用缓存机制存储已解析的 XML 文档，避免重复解析
- **资源缓存**：字体、图片等资源只加载一次，提高访问效率

### 7.2 按需解析

- 支持按需解析 OFD 文档的各个部分，避免一次性加载整个文档
- 提供灵活的 API，允许用户只解析需要的内容

### 7.3 异步支持

- 核心组件设计时考虑了异步操作的支持
- 便于后续扩展异步 API

## 8. 安全性考虑

### 8.1 数字签名支持

- 支持 OFD 文档的数字签名读取和验证
- 支持添加新的数字签名

### 8.2 安全的资源处理

- 对外部资源的访问进行严格的安全检查
- 避免潜在的安全漏洞，如 XML 注入等

### 8.3 权限管理

- 支持 OFD 文档的权限管理
- 支持密码保护的 OFD 文档

## 9. 未来规划

### 9.1 功能扩展

- 支持更多 OFD 元素类型
- 支持 OFD 文档的渲染功能
- 支持 OFD 文档的编辑功能
- 支持 OFD 文档与其他格式的转换

### 9.2 性能优化

- 进一步优化解析和写入性能
- 支持更大尺寸的 OFD 文档
- 优化内存使用

### 9.3 跨平台支持

- 支持更多的 .NET 平台
- 支持 .NET Core 和 .NET Framework
- 支持 Linux 和 macOS 平台

### 9.4 文档完善

- 提供更详细的 API 文档
- 提供更多的使用示例
- 提供更详细的开发指南

## 10. 总结

OFD Viewer 采用分层架构设计，具有良好的可扩展性、可维护性和可测试性。通过模块化的设计，使得系统能够灵活地支持不同版本的 OFD 标准和各种 OFD 元素类型。

未来，OFD Viewer 将继续扩展功能，优化性能，支持更多的平台，为用户提供更全面、更高效的 OFD 文档处理解决方案。