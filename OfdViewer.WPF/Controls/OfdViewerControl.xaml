<UserControl x:Class="OfdViewer.WPF.Controls.OfdViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OfdViewer.WPF.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#F0F0F0" Margin="5">
            <Button Content="打开" Command="{Binding OpenCommand}" Margin="0,0,5,0"/>
            <Button Content="上一页" Command="{Binding PreviousPageCommand}" Margin="0,0,5,0"/>
            <Button Content="下一页" Command="{Binding NextPageCommand}" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <Separator Width="1" Height="20" Background="#CCCCCC" Margin="10,0,10,0"/>
            <Button Content="放大" Command="{Binding ZoomInCommand}" Margin="0,0,5,0"/>
            <Button Content="缩小" Command="{Binding ZoomOutCommand}" Margin="0,0,5,0"/>
            <Button Content="适应窗口" Command="{Binding FitToWindowCommand}"/>
        </StackPanel>
        
        <!-- 图像显示区域 -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#E5E5E5">
            <Viewbox Stretch="Uniform">
                <Grid>
                    <Rectangle Fill="#FFFFFF" Stroke="#CCCCCC" StrokeThickness="1"/>
                    <Image x:Name="OfdImage" Stretch="Uniform"/>
                </Grid>
            </Viewbox>
        </ScrollViewer>
    </Grid>
</UserControl>